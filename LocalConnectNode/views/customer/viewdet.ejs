<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LocalConnect | Vendor Details</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link rel="stylesheet" href="/static/css/customer/base.css">
    <link rel="stylesheet" href="/static/css/customer/viewdet.css">
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <link rel="stylesheet" href="https://unpkg.com/leaflet-routing-machine@3.2.12/dist/leaflet-routing-machine.css" />
    <link rel="stylesheet" href="/static/css/discount_box.css">
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="https://unpkg.com/leaflet-routing-machine@3.2.12/dist/leaflet-routing-machine.js"></script>
    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
</head>

<body data-user-id="<%= user_id  %>">
    <header class="navbar">
        <div class="nav-container">
            <div class="logo" onclick="window.location.href='/customer/dashboard'">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"
                    stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"></path>
                    <circle cx="12" cy="10" r="3"></circle>
                </svg>
                <span>LocalConnect</span>
            </div>

            <div class="search-wrapper" id="searchWrapper">
                <input type="text" id="searchInput" placeholder="Search menu items...">
                <svg class="search-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                    stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="11" cy="11" r="8"></circle>
                    <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                </svg>
            </div>

            <div class="user-profile">
                <span class="welcome-text">Welcome, <%= (typeof user_name !== "undefined" && user_name) ? user_name : 'Guest' %></span>
                <div class="avatar" id="profileIcon">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"></path>
                        <circle cx="12" cy="7" r="4"></circle>
                    </svg>
                </div>
                <div class="profile-dropdown" id="profileDropdown">
                    <div class="dropdown-item" onclick="viewProfile()">
                        <i class="fa-solid fa-user"></i>
                        <span>View Profile</span>
                    </div>
                    <div class="dropdown-item" onclick="myOrders()">
                        <i class="fa-solid fa-shopping-bag"></i>
                        <span>My Orders</span>
                    </div>
                    <div class="dropdown-item" onclick="logout()">
                        <i class="fa-solid fa-sign-out-alt"></i>
                        <span>Logout</span>
                    </div>
                </div>
            </div>
        </div>
    </header>
    <main class="main-content">
        <section class="banner-section">
            <div class="banner-img" style="<% if (vendor.shop_image and vendor.shop_image | replace('\1', '') | trim | length < vendor_profile.shop_image.length) { %>background-image: url('<%= vendor.shop_image  %>');<% } else if (vendor.shop_image and vendor.shop_image not in ['🏪', '🍕', '🍔', '🍜', '🥘', '🍛', '🥗', '☕']) { %>background-image: url('/static/images/shop/<%= vendor.shop_image  %>');<% } else { %>background-image: url('https://images.unsplash.com/photo-1555396273-367ea4eb4db5?auto=format&fit=crop&q=80&w=1200');<% } %>">
                <div class="banner-overlay">
                    <h1><%= vendor.business_name  %></h1>
                </div>
            </div>

            <div class="restaurant-details-card">
                <div class="title-row">
                    <h2><%= vendor.business_name  %></h2>
                    <div class="vendor-highlights" style="display: flex; gap: 12px; margin-top: 15px; flex-wrap: wrap; animation: slideInUp 0.6s ease-out 0.4s both;">
                        <span class="highlight-badge premium-badge" style="background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%); color: white; padding: 8px 16px; border-radius: 25px; font-size: 0.85rem; font-weight: 700; box-shadow: 0 4px 15px rgba(255,107,107,0.3); display: flex; align-items: center; gap: 6px; transition: all 0.3s ease;">⭐ Premium Quality</span>
                        <span class="highlight-badge speed-badge" style="background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%); color: white; padding: 8px 16px; border-radius: 25px; font-size: 0.85rem; font-weight: 700; box-shadow: 0 4px 15px rgba(79,172,254,0.3); display: flex; align-items: center; gap: 6px; transition: all 0.3s ease;">🚀 Fast Service</span>
                        <span class="highlight-badge verified-badge" style="background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%); color: white; padding: 8px 16px; border-radius: 25px; font-size: 0.85rem; font-weight: 700; box-shadow: 0 4px 15px rgba(67,233,123,0.3); display: flex; align-items: center; gap: 6px; transition: all 0.3s ease;">💯 Verified</span>
                    </div>
                    <div class="badge-row">
                        <span class="rating-box pulse-animation"><i class="fa-solid fa-star"></i> <span id="vendor-rating">Loading...</span></span>
                        <span class="distance"><i class="fa-solid fa-location-dot"></i> <span id="vendor-distance">Calculating...</span></span>
                        <span class="type-badge"><%= vendor.business_category  %></span>
                    </div>
                </div>
                <div class="info-row" style="animation: slideInUp 0.6s ease-out 0.6s both;">
                    <span class="status-badge <%= 'status-green' if vendor.is_open else 'status-red'  %>" style="position: relative; overflow: hidden;"><i class="fa-solid fa-circle-<%= 'check' if vendor.is_open else 'xmark'  %>"></i> <%= 'Open Now' if vendor.is_open else 'Closed'  %></span>
                    <% if (vendor.takeaway) { %><span class="service-badge takeaway-badge" style="background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%); color: white; position: relative; overflow: hidden;"><i class="fa-solid fa-bag-shopping"></i> Takeaway</span><% } %>
                    <% if (vendor.home_delivery) { %><span class="service-badge delivery-badge" style="background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%); color: white; position: relative; overflow: hidden;"><i class="fa-solid fa-motorcycle"></i> Delivery</span><% } %>
                    <span class="price-badge" style="background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%); color: #2c3e50; font-weight: 700; position: relative; overflow: hidden;"><i class="fa-solid fa-indian-rupee-sign"></i> <span id="price-range">Loading...</span></span>
                    <span class="map-link enhanced-map-btn" onclick="showMapModal()" style="position: relative; overflow: hidden;"><i class="fa-solid fa-map-location-dot"></i> View on Map</span>
                </div>
            </div>
        </section>

            <div class="tab-container">
                <div class="tabs-nav-inline">
                    <div class="tab active" data-target="menu-section"><% if (vendor.business_category == 'Garage') { %>Services<% } else { %>Menu<% } %></div>
                    <div class="tab" data-target="about-section">About</div>
                    <div class="tab" data-target="reviews-section">Reviews</div>
                </div>
                <section id="menu-section" class="tab-content active">
                    <div class="menu-section-inner" style="padding:30px 5% 120px;background:#f5f7f8;">
                        <div id="menu-grid" class="menu-grid"
                            style="display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:25px;">
                            <!-- Menu items/services will be loaded dynamically -->
                        </div>
                    </div>
                </section>

            <section id="about-section" class="tab-content">
                <div class="about-container">
                    <div class="about-main-card">
                        <!-- Badges Section -->
                        <div style="display:flex;gap:12px;margin-bottom:25px;flex-wrap:wrap;">
                            <% if (vendor.category_type) { %>
                            <span class="info-badge" style="background:linear-gradient(135deg, #667eea 0%, #764ba2 100%);color:white;padding:10px 20px;border-radius:25px;font-weight:600;font-size:0.9rem;box-shadow:0 4px 15px rgba(102,126,234,0.3);">
                                <i class="fa-solid fa-utensils" style="margin-right:8px;"></i><%= vendor.category_type  %>
                            </span>
                            <% } %>
                            <% if (vendor.veg_nonveg) { %>
                            <span class="info-badge" style="background:linear-gradient(135deg, #11998e 0%, #38ef7d 100%);color:white;padding:10px 20px;border-radius:25px;font-weight:600;font-size:0.9rem;box-shadow:0 4px 15px rgba(17,153,142,0.3);">
                                <i class="fa-solid fa-leaf" style="margin-right:8px;"></i><%= vendor.veg_nonveg  %>
                            </span>
                            <% } %>
                        </div>
                        
                        <!-- About Section -->
                        <div style="margin-bottom:35px;">
                            <h3 style="margin-bottom:15px;color:#2c3e50;font-size:1.8rem;font-weight:700;display:flex;align-items:center;gap:10px;">
                                <i class="fa-solid fa-circle-info" style="color:#3498db;font-size:1.5rem;"></i>
                                About <%= vendor.business_name  %>
                            </h3>
                            <p style="line-height:1.9;color:#555;font-size:1.05rem;background:#f8f9fa;padding:20px;border-radius:12px;border-left:4px solid #3498db;"><%= vendor.about if vendor.about else 'No information available.'  %></p>
                        </div>
                        
                        <!-- Amenities Section -->
                        <% if (vendor.indoor_seating or vendor.outdoor_seating or vendor.home_delivery or vendor.takeaway or vendor.free_wifi or vendor.ac or vendor.cooler or vendor.parking or vendor.other_amenities) { %>
                        <div style="margin-bottom:35px;">
                            <h4 style="margin:0 0 25px 0;color:#2c3e50;font-size:1.6rem;font-weight:800;display:flex;align-items:center;gap:12px;">
                                <i class="fa-solid fa-sparkles" style="color:#f39c12;font-size:1.4rem;animation: sparkle 2s infinite;"></i>
                                Amenities & Features
                            </h4>
                            <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:18px;">
                                <% if (vendor.indoor_seating) { %>
                                <div class="amenity-card" style="background:linear-gradient(135deg, #667eea 0%, #764ba2 100%);color:white;padding:18px 22px;border-radius:15px;display:flex;align-items:center;gap:14px;box-shadow:0 6px 20px rgba(102,126,234,0.25);transition:all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);position:relative;overflow:hidden;">
                                    <i class="fa-solid fa-couch" style="font-size:1.8rem;z-index:2;"></i>
                                    <span style="font-weight:700;font-size:1rem;z-index:2;">Indoor Seating</span>
                                    <div class="amenity-shine" style="position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:linear-gradient(45deg,transparent,rgba(255,255,255,0.1),transparent);transform:rotate(45deg);transition:all 0.6s ease;"></div>
                                </div>
                                <% } %>
                                <% if (vendor.outdoor_seating) { %>
                                <div class="amenity-card" style="background:linear-gradient(135deg, #f093fb 0%, #f5576c 100%);color:white;padding:18px 22px;border-radius:15px;display:flex;align-items:center;gap:14px;box-shadow:0 6px 20px rgba(240,147,251,0.25);transition:all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);position:relative;overflow:hidden;">
                                    <i class="fa-solid fa-wind" style="font-size:1.8rem;z-index:2;"></i>
                                    <span style="font-weight:700;font-size:1rem;z-index:2;">Outdoor Seating</span>
                                    <div class="amenity-shine" style="position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:linear-gradient(45deg,transparent,rgba(255,255,255,0.1),transparent);transform:rotate(45deg);transition:all 0.6s ease;"></div>
                                </div>
                                <% } %>
                                <% if (vendor.home_delivery) { %>
                                <div class="amenity-card" style="background:linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);color:white;padding:18px 22px;border-radius:15px;display:flex;align-items:center;gap:14px;box-shadow:0 6px 20px rgba(79,172,254,0.25);transition:all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);position:relative;overflow:hidden;">
                                    <i class="fa-solid fa-truck-ramp-box" style="font-size:1.8rem;z-index:2;"></i>
                                    <span style="font-weight:700;font-size:1rem;z-index:2;">Home Delivery</span>
                                    <div class="amenity-shine" style="position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:linear-gradient(45deg,transparent,rgba(255,255,255,0.1),transparent);transform:rotate(45deg);transition:all 0.6s ease;"></div>
                                </div>
                                <% } %>
                                <% if (vendor.takeaway) { %>
                                <div class="amenity-card" style="background:linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);color:white;padding:18px 22px;border-radius:15px;display:flex;align-items:center;gap:14px;box-shadow:0 6px 20px rgba(67,233,123,0.25);transition:all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);position:relative;overflow:hidden;">
                                    <i class="fa-solid fa-bag-shopping" style="font-size:1.8rem;z-index:2;"></i>
                                    <span style="font-weight:700;font-size:1rem;z-index:2;">Takeaway</span>
                                    <div class="amenity-shine" style="position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:linear-gradient(45deg,transparent,rgba(255,255,255,0.1),transparent);transform:rotate(45deg);transition:all 0.6s ease;"></div>
                                </div>
                                <% } %>
                                <% if (vendor.free_wifi) { %>
                                <div class="amenity-card" style="background:linear-gradient(135deg, #fa709a 0%, #fee140 100%);color:white;padding:18px 22px;border-radius:15px;display:flex;align-items:center;gap:14px;box-shadow:0 6px 20px rgba(250,112,154,0.25);transition:all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);position:relative;overflow:hidden;">
                                    <i class="fa-solid fa-wifi" style="font-size:1.8rem;z-index:2;"></i>
                                    <span style="font-weight:700;font-size:1rem;z-index:2;">Free Wi-Fi</span>
                                    <div class="amenity-shine" style="position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:linear-gradient(45deg,transparent,rgba(255,255,255,0.1),transparent);transform:rotate(45deg);transition:all 0.6s ease;"></div>
                                </div>
                                <% } %>
                                <% if (vendor.ac) { %>
                                <div class="amenity-card" style="background:linear-gradient(135deg, #30cfd0 0%, #330867 100%);color:white;padding:18px 22px;border-radius:15px;display:flex;align-items:center;gap:14px;box-shadow:0 6px 20px rgba(48,207,208,0.25);transition:all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);position:relative;overflow:hidden;">
                                    <i class="fa-solid fa-snowflake" style="font-size:1.8rem;z-index:2;"></i>
                                    <span style="font-weight:700;font-size:1rem;z-index:2;">AC</span>
                                    <div class="amenity-shine" style="position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:linear-gradient(45deg,transparent,rgba(255,255,255,0.1),transparent);transform:rotate(45deg);transition:all 0.6s ease;"></div>
                                </div>
                                <% } %>
                                <% if (vendor.cooler) { %>
                                <div class="amenity-card" style="background:linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);color:#2c3e50;padding:18px 22px;border-radius:15px;display:flex;align-items:center;gap:14px;box-shadow:0 6px 20px rgba(168,237,234,0.25);transition:all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);position:relative;overflow:hidden;">
                                    <i class="fa-solid fa-fan" style="font-size:1.8rem;z-index:2;"></i>
                                    <span style="font-weight:700;font-size:1rem;z-index:2;">Cooler</span>
                                    <div class="amenity-shine" style="position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:linear-gradient(45deg,transparent,rgba(255,255,255,0.1),transparent);transform:rotate(45deg);transition:all 0.6s ease;"></div>
                                </div>
                                <% } %>
                                <% if (vendor.parking) { %>
                                <div class="amenity-card" style="background:linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);color:#2c3e50;padding:18px 22px;border-radius:15px;display:flex;align-items:center;gap:14px;box-shadow:0 6px 20px rgba(255,154,158,0.25);transition:all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);position:relative;overflow:hidden;">
                                    <i class="fa-solid fa-square-parking" style="font-size:1.8rem;z-index:2;"></i>
                                    <span style="font-weight:700;font-size:1rem;z-index:2;">Parking <%= vendor.parking  %></span>
                                    <div class="amenity-shine" style="position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:linear-gradient(45deg,transparent,rgba(255,255,255,0.1),transparent);transform:rotate(45deg);transition:all 0.6s ease;"></div>
                                </div>
                                <% } %>
                                <% if (vendor.other_amenities) { %>
                                    <% if (vendor.other_amenities.split(',')) { (vendor.other_amenities.split(',')).forEach(amenity => { %>
                                    <div class="amenity-card" style="background:linear-gradient(135deg, #ffeaa7 0%, #fdcb6e 100%);color:#2c3e50;padding:18px 22px;border-radius:15px;display:flex;align-items:center;gap:14px;box-shadow:0 6px 20px rgba(253,203,110,0.25);transition:all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);position:relative;overflow:hidden;">
                                        <i class="fa-solid fa-check-circle" style="font-size:1.8rem;z-index:2;"></i>
                                        <span style="font-weight:700;font-size:1rem;z-index:2;"><%= amenity|trim  %></span>
                                        <div class="amenity-shine" style="position:absolute;top:-50%;left:-50%;width:200%;height:200%;background:linear-gradient(45deg,transparent,rgba(255,255,255,0.1),transparent);transform:rotate(45deg);transition:all 0.6s ease;"></div>
                                    </div>
                                    <% }); } %>
                                <% } %>
                            </div>
                        </div>
                        <% } %>

                        <!-- Contact Information -->
                        <div>
                            <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:20px;">
                                <% if (vendor.opening_time and vendor.closing_time) { %>
                                <div style="background:linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);padding:20px;border-radius:12px;box-shadow:0 4px 15px rgba(252,182,159,0.2);">
                                    <h5 style="margin:0 0 10px 0;color:#d35400;font-size:1rem;display:flex;align-items:center;gap:8px;">
                                        <i class="fa-solid fa-clock"></i> Timings
                                    </h5>
                                    <p style="margin:0;color:#2c3e50;font-weight:600;font-size:1.1rem;"><%= vendor.opening_time  %> - <%= vendor.closing_time  %></p>
                                </div>
                                <% } %>
                                <div style="background:linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%);padding:20px;border-radius:12px;box-shadow:0 4px 15px rgba(161,196,253,0.2);">
                                    <h5 style="margin:0 0 10px 0;color:#2980b9;font-size:1rem;display:flex;align-items:center;gap:8px;">
                                        <i class="fa-solid fa-phone"></i> Contact
                                    </h5>
                                    <p style="margin:0;color:#2c3e50;font-weight:600;font-size:1.1rem;"><%= vendor.phone  %></p>
                                </div>
                                <div style="background:linear-gradient(135deg, #ffeaa7 0%, #fdcb6e 100%);padding:20px;border-radius:12px;box-shadow:0 4px 15px rgba(253,203,110,0.2);">
                                    <h5 style="margin:0 0 10px 0;color:#d63031;font-size:1rem;display:flex;align-items:center;gap:8px;">
                                        <i class="fa-solid fa-location-dot"></i> Address
                                    </h5>
                                    <p style="margin:0;color:#2c3e50;font-weight:600;font-size:1.05rem;"><%= vendor.business_address  %></p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>

            <section id="reviews-section" class="tab-content">
                <div class="reviews-container">
                    <div class="review-header">
                        <div class="big-rating" id="reviews-avg-rating">Loading...</div>
                        <div class="stat-text">Based on <span id="reviews-total-count">0</span> verified reviews</div>
                    </div>
                    <div id="reviews-list">
                        <!-- Reviews will be loaded dynamically -->
                    </div>
                </div>
            </section>
        </div>
    </main>

    <div id="cart-footer" class="cart-footer hidden">
        <div class="cart-info"><span id="cart-count">0 items</span> | <span id="cart-total">₹0</span></div>
        <button class="view-cart-btn" onclick="openCart()">View Cart</button>
    </div>

    <!-- Cart Modal -->
    <div id="cart-modal" class="cart-modal">
        <div class="cart-content">
            <div class="cart-header">
                <h2>Your Cart</h2>
                <button class="close-cart" onclick="closeCart()">&times;</button>
            </div>
            <div id="cart-items-list"></div>
            <div class="cart-total">
                <h3 id="modal-total">₹0</h3>
                <button class="buy-btn" onclick="proceedToBuy()">Proceed to Buy</button>
            </div>
        </div>
    </div>

    <div class="chat-button" id="chatBtn">
        <div class="chat-pulse"></div>
        <svg width="32" height="32" viewBox="0 0 24 24" fill="white">
            <path
                d="M12 2a10 10 0 0 0-10 10c0 2.5.9 4.8 2.4 6.6l-.4 3.4 3.4-.4c1.8 1.5 4.1 2.4 6.6 2.4a10 10 0 0 0 10-10A10 10 0 0 0 12 2zm-3 9a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3zm6 0a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3z">
            </path>
        </svg>
    </div>

    <!-- Map Modal -->
    <div id="map-modal" class="cart-modal">
        <div class="cart-content" style="max-width:900px;">
            <div class="cart-header">
                <h2>Location & Directions</h2>
                <button class="close-cart" onclick="closeMapModal()">&times;</button>
            </div>
            <div style="padding:20px;">
                <div id="map" style="width:100%;height:500px;border-radius:12px;"></div>
            </div>
        </div>
    </div>

    <!-- Include Discount Box -->
    <%- include("components/discount_box.ejs") %>

    <script src="/static/js/discount_box.js"></script>
    <script src="/static/js/customer/base.js"></script>
    <script>
        // Set vendor ID for discount box
        window.currentVendorId = <%= vendor_id  %>;
        // Global functions for profile dropdown
        function viewProfile() {
            window.location.href = '/customer/profile';
        }
        
        function myOrders() {
            window.location.href = '/customer/orders';
        }
        
        function logout() {
            // Clear any local storage or session storage
            if (typeof(Storage) !== "undefined") {
                localStorage.clear();
                sessionStorage.clear();
            }
            
            // Redirect to logout endpoint
            window.location.href = '/logout';
        }
        
        const userId = '<%= user_id if user_id else ""  %>';
        const vendorId = '<%= vendor.id if vendor else (vendor_id if vendor_id else "")  %>';
        const vendorName = '<%= vendor.business_name if vendor else ""  %>';

        // Clear ALL localStorage if different user logs in
        const lastUserId = localStorage.getItem('lastUserId');
        if (lastUserId && lastUserId !== userId && userId) {
            localStorage.clear();
        }
        if (userId) localStorage.setItem('lastUserId', userId);

        const ordersKey = userId ? `user_${userId}_orders` : 'orders';
        const initialMenuItems = <%= (menu_items | tojson) if menu_items else '[]'  %>;
        let menuItems = Array.isArray(initialMenuItems) ? initialMenuItems.map(item => ({
            id: item.id || item._id,
            name: item.name,
            sub_name: item.sub_name || item.subName || '',
            category: item.category || '',
            price: item.price,
            qty: 0,
            available: item.is_available !== undefined ? item.is_available : (item.isAvailable !== undefined ? item.isAvailable : true),
            image_file: item.image_file || item.imageFile || 'default.jpg'
        })) : [];

        function updatePriceRange() {
            if (menuItems.length > 0) {
                const prices = menuItems.map(i => i.price);
                const minPrice = Math.min(...prices);
                const maxPrice = Math.max(...prices);
                const priceEl = document.getElementById('price-range');
                if (priceEl) priceEl.textContent = `${minPrice}—${maxPrice}`;
            } else {
                const priceEl = document.getElementById('price-range');
                if (priceEl) priceEl.textContent = 'N/A';
            }
        }

        updatePriceRange();
        renderMenuCards();

        // Also fetch from API to ensure live sync
        if (vendorId) {
            fetch(`/api/vendor/${vendorId}/menu`)
                .then(res => res.json())
                .then(items => {
                    if (Array.isArray(items) && items.length > 0) {
                        menuItems = items.map(item => ({
                            id: item.id || item._id,
                            name: item.name,
                            sub_name: item.sub_name || item.subName || '',
                            category: item.category || '',
                            price: item.price,
                            qty: 0,
                            available: item.is_available !== undefined ? item.is_available : (item.isAvailable !== undefined ? item.isAvailable : true),
                            image_file: item.image_file || item.imageFile || 'default.jpg'
                        }));
                        updatePriceRange();
                        renderMenuCards();
                    }
                })
                .catch(err => console.log('API menu load:', err));
        }

        // Calculate distance if vendor has coordinates
        const vendorLat = <%= vendor.latitude if vendor.latitude else 'null'  %>;
        const vendorLon = <%= vendor.longitude if vendor.longitude else 'null'  %>;
        
        if (vendorLat && vendorLon && navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(
                (position) => {
                    const distance = calculateDistance(
                        position.coords.latitude,
                        position.coords.longitude,
                        vendorLat,
                        vendorLon
                    );
                    document.getElementById('vendor-distance').textContent = distance.toFixed(1) + ' km away';
                },
                () => {
                    document.getElementById('vendor-distance').textContent = 'Location unavailable';
                }
            );
        } else {
            document.getElementById('vendor-distance').textContent = 'N/A';
        }

        function calculateDistance(lat1, lon1, lat2, lon2) {
            const R = 6371;
            const dLat = (lat2 - lat1) * Math.PI / 180;
            const dLon = (lon2 - lon1) * Math.PI / 180;
            const a = Math.sin(dLat/2) * Math.sin(dLat/2) +
                Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
                Math.sin(dLon/2) * Math.sin(dLon/2);
            const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
            return R * c;
        }

        function openGoogleMaps() {
            const vendorLat = <%= vendor.latitude if vendor.latitude else 'null'  %>;
            const vendorLon = <%= vendor.longitude if vendor.longitude else 'null'  %>;
            
            if (!vendorLat || !vendorLon) {
                alert('Location not available for this vendor.');
                return;
            }
            
            if (navigator.geolocation) {
                navigator.geolocation.getCurrentPosition(
                    (position) => {
                        const userLat = position.coords.latitude;
                        const userLon = position.coords.longitude;
                        const url = `https://www.google.com/maps/dir/?api=1&origin=${userLat},${userLon}&destination=${vendorLat},${vendorLon}&travelmode=driving`;
                        window.open(url, '_blank');
                    },
                    () => {
                        const url = `https://www.google.com/maps/search/?api=1&query=${vendorLat},${vendorLon}`;
                        window.open(url, '_blank');
                    }
                );
            } else {
                const url = `https://www.google.com/maps/search/?api=1&query=${vendorLat},${vendorLon}`;
                window.open(url, '_blank');
            }
        }

        let mapInstance = null;
        let routingControl = null;

        function showMapModal() {
            const vendorLat = <%= vendor.latitude if vendor.latitude else 'null'  %>;
            const vendorLon = <%= vendor.longitude if vendor.longitude else 'null'  %>;
            
            if (!vendorLat || !vendorLon) {
                alert('Location not available for this vendor.');
                return;
            }
            
            document.getElementById('map-modal').classList.add('active');
            
            setTimeout(() => {
                if (!mapInstance) {
                    mapInstance = L.map('map').setView([vendorLat, vendorLon], 13);
                    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
                        attribution: '© OpenStreetMap contributors'
                    }).addTo(mapInstance);
                }
                
                if (routingControl) {
                    mapInstance.removeControl(routingControl);
                }
                
                if (navigator.geolocation) {
                    navigator.geolocation.getCurrentPosition(
                        (position) => {
                            const userLat = position.coords.latitude;
                            const userLon = position.coords.longitude;
                            
                            routingControl = L.Routing.control({
                                waypoints: [
                                    L.latLng(userLat, userLon),
                                    L.latLng(vendorLat, vendorLon)
                                ],
                                routeWhileDragging: false,
                                show: false,
                                addWaypoints: false,
                                lineOptions: {
                                    styles: [{ color: '#27ae60', weight: 5 }]
                                },
                                createMarker: function(i, wp) {
                                    return L.marker(wp.latLng, {
                                        icon: L.icon({
                                            iconUrl: i === 0 ? 'https://raw.githubusercontent.com/pointhi/leaflet-color-markers/master/img/marker-icon-2x-blue.png' : 'https://raw.githubusercontent.com/pointhi/leaflet-color-markers/master/img/marker-icon-2x-red.png',
                                            shadowUrl: 'https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/images/marker-shadow.png',
                                            iconSize: [25, 41],
                                            iconAnchor: [12, 41],
                                            popupAnchor: [1, -34],
                                            shadowSize: [41, 41]
                                        })
                                    }).bindPopup(i === 0 ? '<b>Your Location</b>' : '<b><%= vendor.business_name  %></b><br><%= vendor.business_address  %>');
                                }
                            }).addTo(mapInstance);
                            
                            routingControl.on('routesfound', function(e) {
                                const route = e.routes[0];
                                const distance = (route.summary.totalDistance / 1000).toFixed(1);
                                const time = Math.round(route.summary.totalTime / 60);
                                L.popup()
                                    .setLatLng([vendorLat, vendorLon])
                                    .setContent(`<b><%= vendor.business_name  %></b><br>Distance: ${distance} km<br>Time: ${time} min`)
                                    .openOn(mapInstance);
                            });
                        },
                        () => {
                            L.marker([vendorLat, vendorLon]).addTo(mapInstance)
                                .bindPopup(`<b><%= vendor.business_name  %></b><br><%= vendor.business_address  %>`).openPopup();
                            mapInstance.setView([vendorLat, vendorLon], 15);
                        }
                    );
                } else {
                    L.marker([vendorLat, vendorLon]).addTo(mapInstance)
                        .bindPopup(`<b><%= vendor.business_name  %></b><br><%= vendor.business_address  %>`).openPopup();
                    mapInstance.setView([vendorLat, vendorLon], 15);
                }
            }, 100);
        }

        function closeMapModal() {
            document.getElementById('map-modal').classList.remove('active');
        }

        function openGoogleMaps() {
            const vendorLat = <%= vendor.latitude if vendor.latitude else 'null'  %>;
            const vendorLon = <%= vendor.longitude if vendor.longitude else 'null'  %>;
            
            if (!vendorLat || !vendorLon) {
                alert('Location not available for this vendor.');
                return;
            }
            
            if (navigator.geolocation) {
                navigator.geolocation.getCurrentPosition(
                    (position) => {
                        const userLat = position.coords.latitude;
                        const userLon = position.coords.longitude;
                        const mapUrl = `https://www.google.com/maps/dir/?api=1&origin=${userLat},${userLon}&destination=${vendorLat},${vendorLon}&travelmode=driving`;
                        window.open(mapUrl, '_blank');
                    },
                    () => {
                        const mapUrl = `https://www.google.com/maps/search/?api=1&query=${vendorLat},${vendorLon}`;
                        window.open(mapUrl, '_blank');
                    }
                );
            } else {
                const mapUrl = `https://www.google.com/maps/search/?api=1&query=${vendorLat},${vendorLon}`;
                window.open(mapUrl, '_blank');
            }
        }

        document.querySelectorAll('.tab').forEach(tab => {
            tab.addEventListener('click', () => {
                document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
                document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
                tab.classList.add('active');
                document.getElementById(tab.getAttribute('data-target')).classList.add('active');
            });
        });

        document.getElementById('searchInput').addEventListener('input', (e) => {
            const term = e.target.value.toLowerCase();
            document.querySelectorAll('.menu-card').forEach(card => {
                const name = card.querySelector('h4').textContent.toLowerCase();
                card.style.display = name.includes(term) ? 'flex' : 'none';
            });
        });

        function renderMenuCards() {
            const isOpen = <%= 'true' if vendor.is_open else 'false'  %>;
            const isGarage = '<%= vendor.business_category  %>' === 'Garage';
            const menuGrid = document.getElementById('menu-grid');
            menuGrid.innerHTML = menuItems.map(item => {
                const imageUrl = item.image_file && item.image_file !== 'default.jpg'
                    ? (item.image_file.startsWith('http') || item.image_file.startsWith('data:') ? item.image_file : `/static/images/food/${item.image_file}`)
                    : 'https://via.placeholder.com/400x300?text=No+Image';
                return `
                <div class="menu-card" style="background:white;border-radius:15px;padding:15px;border:1px solid #eee;display:flex;flex-direction:column;box-shadow:0 4px 15px rgba(0,0,0,0.04);${!item.available || !isOpen ? 'opacity:0.6;' : ''}">
                    <img src="${imageUrl}" alt="${item.name}" class="item-img" style="width:100%;height:180px;object-fit:cover;border-radius:10px;margin-bottom:15px;" onerror="this.src='https://via.placeholder.com/400x300?text=No+Image'">
                    <h4 style="margin:0 0 8px 0;">${item.name}</h4>
                    <p style="margin:0 0 10px 0;color:#7f8c8d;font-size:14px;">${item.sub_name || ''}</p>
                    <div class="action-row" style="display:flex;justify-content:space-between;align-items:center;margin-top:auto;">
                        ${!isOpen ?
                        '<span style="font-weight:700;color:#e74c3c;font-size:14px;">Shop Closed</span>' :
                        !item.available ?
                        '<span style="font-weight:700;color:#e74c3c;font-size:14px;">Not Available</span>' :
                        isGarage ?
                            `<span style="font-weight:700;color:#27ae60;font-size:18px;">₹${item.price}</span><button class="btn-add" onclick="bookService(${item.id})" style="background:#27ae60;color:white;border:none;padding:8px 16px;border-radius:8px;cursor:pointer;">Book</button>` :
                        item.qty > 0 ?
                            `<span style="font-weight:700;color:#27ae60;font-size:18px;">₹${item.price}</span><div style="display:flex;align-items:center;background:#27ae60;color:white;border-radius:8px;padding:4px 8px;gap:10px;"><button onclick="updateQty(${item.id}, -1)" style="background:none;border:none;color:white;cursor:pointer;font-size:16px;">-</button><span style="font-weight:700;">${item.qty}</span><button onclick="updateQty(${item.id}, 1)" style="background:none;border:none;color:white;cursor:pointer;font-size:16px;">+</button></div>` :
                            `<span style="font-weight:700;color:#27ae60;font-size:18px;">₹${item.price}</span><button class="btn-add" onclick="addToCart(${item.id})" style="background:#27ae60;color:white;border:none;padding:8px 16px;border-radius:8px;cursor:pointer;">Add</button>`
                    }
                    </div>
                </div>
            `}).join('');
        }

        function addToCart(itemId) {
            const isOpen = <%= 'true' if vendor.is_open else 'false'  %>;
            if (!isOpen) {
                alert('Sorry, this shop is currently closed. You cannot place orders at this time.');
                return;
            }
            const item = menuItems.find(i => i.id === itemId);
            if (!item.available) {
                alert('Sorry, this item is currently not available.');
                return;
            }
            item.qty++;
            updateCart();
            renderMenuCards();
        }

        function bookService(itemId) {
            addToCart(itemId);
        }

        function updateQty(itemId, change) {
            const item = menuItems.find(i => i.id === itemId);
            item.qty = Math.max(0, item.qty + change);
            updateCart();
            renderMenuCards();
            
            // Update cart modal if it's open
            if (document.getElementById('cart-modal').classList.contains('active')) {
                openCart();
            }
        }

        function updateCart() {
            const count = menuItems.reduce((s, i) => s + i.qty, 0);
            const total = menuItems.reduce((s, i) => s + (i.qty * i.price), 0);
            if (count > 0) {
                document.getElementById('cart-footer').classList.remove('hidden');
                document.getElementById('cart-count').textContent = `${count} items`;
                document.getElementById('cart-total').textContent = `₹${total}`;
            } else {
                document.getElementById('cart-footer').classList.add('hidden');
            }
        }

        function openCart() {
            const cartItems = menuItems.filter(i => i.qty > 0);
            const total = cartItems.reduce((s, i) => s + (i.qty * i.price), 0);
            const cartItemsList = document.getElementById('cart-items-list');
            const modalTotal = document.getElementById('modal-total');

            if (cartItems.length === 0) {
                cartItemsList.innerHTML = '<div class="empty-cart"><i class="fa-solid fa-shopping-cart"></i><h3>Your cart is empty</h3><p>Add some delicious items to get started!</p></div>';
                modalTotal.style.display = 'none';
            } else {
                const cashRestricted = total > 200;
                const paymentMessage = cashRestricted ? 
                    '<div style="background:#fff3cd;border:1px solid #ffeaa7;color:#856404;padding:12px;border-radius:8px;margin:15px 0;font-size:14px;"><i class="fa-solid fa-info-circle"></i> <strong>Note:</strong> Orders above ₹200 require online payment only. Cash payment is available for orders ₹200 and below.</div>' : '';
                
                cartItemsList.innerHTML = cartItems.map(item => `
                    <div class="cart-item">
                        <div class="cart-item-info"><h4>${item.name}</h4><p>₹${item.price} each</p></div>
                        <div class="cart-item-controls">
                            <div style="display:flex;align-items:center;background:#27ae60;color:white;border-radius:8px;padding:4px 8px;gap:10px;">
                                <button onclick="updateQty(${item.id}, -1)" style="background:none;border:none;color:white;cursor:pointer;font-size:16px;">-</button>
                                <span style="font-weight:700;min-width:20px;text-align:center;">${item.qty}</span>
                                <button onclick="updateQty(${item.id}, 1)" style="background:none;border:none;color:white;cursor:pointer;font-size:16px;">+</button>
                            </div>
                            <span style="font-weight:600;color:#27ae60;">₹${item.qty * item.price}</span>
                        </div>
                    </div>
                `).join('') + paymentMessage;
                modalTotal.innerHTML = `Total: ₹${total}`;
                modalTotal.style.display = 'block';
            }
            document.getElementById('cart-modal').classList.add('active');
        }

        function closeCart() {
            document.getElementById('cart-modal').classList.remove('active');
        }

        function proceedToBuy() {
            const cartItems = menuItems.filter(i => i.qty > 0);
            if (cartItems.length === 0) { alert('Your cart is empty!'); return; }
            const total = cartItems.reduce((s, i) => s + (i.qty * i.price), 0);
            
            const isGarage = '<%= vendor.business_category  %>' === 'Garage';
            if (isGarage) {
                showLocationChoice(total);
            } else {
                showDeliverySelection(total);
            }
        }

        function showDeliverySelection(total) {
            document.getElementById('cart-items-list').innerHTML = `
                <div class="order-step"><h3>Choose Delivery Option</h3>
                    <div class="delivery-options">
                        <div class="delivery-option" onclick="selectDelivery('takeaway', ${total})">
                            <div class="option-icon">🏪</div>
                            <div class="option-details"><h4>Takeaway</h4><p>Ready in 15-20 minutes</p><span class="price">Free</span></div>
                        </div>
                        <div class="delivery-option" onclick="selectDelivery('delivery', ${total})">
                            <div class="option-icon">🚚</div>
                            <div class="option-details"><h4>Home Delivery</h4><p>Delivered in 25-30 minutes</p><span class="price">₹30</span></div>
                        </div>
                    </div>
                </div>`;
            document.getElementById('modal-total').innerHTML = `Subtotal: ₹${total}`;
        }

        function selectDelivery(type, subtotal) {
            if (type === 'delivery') {
                // Show location choice for home delivery
                showLocationChoice(subtotal);
            } else {
                // Proceed with takeaway (no location choice needed)
                window.selectedLocationType = null;
                proceedWithDeliveryType(type, subtotal);
            }
        }
        
        function showLocationChoice(subtotal) {
            document.getElementById('cart-items-list').innerHTML = `
                <div class="order-step"><h3>Choose Delivery Location</h3>
                    <div class="delivery-options">
                        <div class="delivery-option" onclick="selectLocation('home', ${subtotal})">
                            <div class="option-icon">🏠</div>
                            <div class="option-details"><h4>Home Location</h4><p>Deliver to your saved home address</p></div>
                        </div>
                        <div class="delivery-option" onclick="selectLocation('current', ${subtotal})">
                            <div class="option-icon">📍</div>
                            <div class="option-details"><h4>Current Location</h4><p>Deliver to your current GPS location</p></div>
                        </div>
                    </div>
                </div>`;
            document.getElementById('modal-total').innerHTML = `Subtotal: ₹${subtotal}`;
        }
        
        function selectLocation(locationType, subtotal) {
            // Store selected location type for order processing
            window.selectedLocationType = locationType;
            const isGarage = '<%= vendor.business_category  %>' === 'Garage';
            if (isGarage) {
                proceedWithDeliveryType('delivery', subtotal);
            } else {
                proceedWithDeliveryType('delivery', subtotal);
            }
        }
        
        function proceedWithDeliveryType(type, subtotal) {
            const isGarage = '<%= vendor.business_category  %>' === 'Garage';
            const deliveryType = isGarage ? 'delivery' : type;
            const deliveryFee = deliveryType === 'delivery' ? 30 : 0;
            
            // Calculate discount if applicable
            let discount = 0;
            let appliedOffer = null;
            
            if (vendorOffers && vendorOffers.length > 0) {
                for (let offer of vendorOffers) {
                    if (subtotal >= offer.min_order) {
                        const offerDiscount = offer.discount_type === 'percentage' 
                            ? (subtotal * offer.discount_value / 100)
                            : offer.discount_value;
                        if (offerDiscount > discount) {
                            discount = offerDiscount;
                            appliedOffer = offer;
                        }
                    }
                }
            }
            
            const discountedSubtotal = subtotal - discount;
            const total = discountedSubtotal + deliveryFee;
            
            // Check if total is above 200 for cash payment restriction
            const cashRestricted = total > 200;
            const cashMessage = cashRestricted ? 
                '<div style="background:#fff3cd;border:1px solid #ffeaa7;color:#856404;padding:12px;border-radius:8px;margin:15px 0;font-size:14px;"><i class="fa-solid fa-info-circle"></i> <strong>Note:</strong> Orders above ₹200 require online payment only. Cash payment is available for orders ₹200 and below.</div>' : '';
            
            const discountHtml = discount > 0 ? 
                `<div class="summary-row discount-row" style="color:#27ae60;"><span>Discount (${appliedOffer.title}):</span><span>-₹${discount.toFixed(0)}</span></div>` : '';
            
            const serviceLabel = isGarage ? 'Service Fee:' : (deliveryType === 'delivery' ? 'Delivery Fee:' : 'Takeaway:');
            
            document.getElementById('cart-items-list').innerHTML = `
                <div class="order-step"><h3>Payment Method</h3>
                    <div class="order-summary">
                        <div class="summary-row"><span>Subtotal:</span><span>₹${subtotal}</span></div>
                        ${discountHtml}
                        <div class="summary-row"><span>${serviceLabel}</span><span>${deliveryFee > 0 ? '₹' + deliveryFee : 'Free'}</span></div>
                        <div class="summary-row total-row"><span>Total:</span><span>₹${total.toFixed(0)}</span></div>
                    </div>
                    ${cashMessage}
                    <div style="margin:20px 0;">
                        <label style="display:block;margin-bottom:8px;font-weight:600;color:#2c3e50;">Special Instructions (Optional):</label>
                        <textarea id="customerSuggestion" placeholder="Any special requests or instructions for the vendor..." style="width:100%;padding:12px;border:1px solid #ddd;border-radius:8px;min-height:80px;resize:vertical;font-family:inherit;"></textarea>
                    </div>
                    <div class="payment-options">
                        <div class="payment-option" onclick="if(!window.processingPayment) processPayment('online', '${deliveryType}', ${total}, ${discount}, '${appliedOffer ? appliedOffer.title : ''}')">
                            <div class="option-icon">💳</div>
                            <div class="option-details"><h4>Online Payment</h4><p>Pay now with card/UPI</p></div>
                        </div>
                        <div class="payment-option ${cashRestricted ? 'disabled' : ''}" ${cashRestricted ? '' : `onclick="if(!window.processingPayment) processPayment('cash', '${deliveryType}', ${total}, ${discount}, '${appliedOffer ? appliedOffer.title : ''}')"`}>
                            <div class="option-icon">💵</div>
                            <div class="option-details"><h4>Cash on ${isGarage ? 'Service' : (deliveryType === 'delivery' ? 'Delivery' : 'Pickup')}</h4><p>${cashRestricted ? 'Not available for orders above ₹200' : 'Pay when you receive'}</p></div>
                        </div>
                    </div>
                </div>`;
            document.getElementById('modal-total').style.display = 'none';
        }

        function processPayment(paymentType, deliveryType, total, discount = 0, offerTitle = '') {
            // Prevent multiple clicks
            if (window.processingPayment) return;
            window.processingPayment = true;
            
            const cartItems = menuItems.filter(i => i.qty > 0);
            const customerSuggestion = document.getElementById('customerSuggestion')?.value?.trim() || '';
            const orderData = {
                vendor_id: vendorId,
                vendor: vendorName,
                items: cartItems.map(i => ({ name: i.name, qty: i.qty, price: i.price })),
                deliveryType: deliveryType,
                paymentType: paymentType,
                total: total,
                discount: discount,
                offerTitle: offerTitle,
                customerSuggestion: customerSuggestion,
                locationType: window.selectedLocationType || null
            };

            fetch('/api/orders', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(orderData)
            })
            .then(res => {
                if (!res.ok) {
                    return res.text().then(text => {
                        console.error('Order creation failed:', res.status, text);
                        throw new Error(`HTTP ${res.status}: ${text}`);
                    });
                }
                return res.json();
            })
            .then(data => {
                window.processingPayment = false;
                console.log('Order creation response:', data);
                if (data.success) {
                    const orderId = data.order_id;
                    if (paymentType === 'cash') {
                        finalizeOrder(paymentType, deliveryType, total);
                    } else {
                        // Create Razorpay order
                        fetch('/api/payment/create', {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({ order_id: orderId })
                        })
                        .then(res => res.json())
                        .then(paymentData => {
                            if (paymentData.error) {
                                window.processingPayment = false;
                                alert('Error initiating payment: ' + paymentData.error);
                                return;
                            }

                            const options = {
                                "key": paymentData.razorpay_key_id,
                                "amount": paymentData.amount,
                                "currency": "INR",
                                "name": "LocalConnect",
                                "description": "Payment for " + vendorName,
                                "order_id": paymentData.razorpay_order_id,
                                "handler": function (response) {
                                    // Verify payment
                                    fetch('/api/payment/verify', {
                                        method: 'POST',
                                        headers: { 'Content-Type': 'application/json' },
                                        body: JSON.stringify({
                                            order_id: orderId,
                                            razorpay_order_id: response.razorpay_order_id,
                                            razorpay_payment_id: response.razorpay_payment_id,
                                            razorpay_signature: response.razorpay_signature
                                        })
                                    })
                                    .then(res => res.json())
                                    .then(verifyData => {
                                        window.processingPayment = false;
                                        if (verifyData.success) {
                                            finalizeOrder(paymentType, deliveryType, total);
                                        } else {
                                            alert('Payment verification failed: ' + (verifyData.error || 'Unknown error'));
                                        }
                                    })
                                    .catch(err => {
                                        window.processingPayment = false;
                                        alert('Error verifying payment: ' + err.message);
                                    });
                                },
                                "prefill": {
                                    "name": paymentData.customer_name,
                                    "email": paymentData.customer_email,
                                    "contact": paymentData.customer_phone
                                },
                                "theme": {
                                    "color": "#27ae60"
                                },
                                "modal": {
                                    "ondismiss": function() {
                                        window.processingPayment = false;
                                        console.log('Payment modal closed');
                                    }
                                }
                            };
                            const rzp1 = new Razorpay(options);
                            rzp1.open();
                        })
                        .catch(error => {
                            window.processingPayment = false;
                            alert('Order created, but payment initiation failed.');
                        });
                    }
                } else {
                    console.error('Order creation failed:', data);
                    alert('Error creating order: ' + (data.error || 'Unknown error'));
                }
            })
            .catch(error => {
                window.processingPayment = false;
                console.error('Order creation error:', error);
                alert('Failed to create order: ' + error.message);
            });
        }

        function finalizeOrder(paymentType, deliveryType, total) {
            menuItems.forEach(i => i.qty = 0);
            updateCart();
            renderMenuCards();
            closeCart();
            const isGarage = '<%= vendor.business_category  %>' === 'Garage';
            const paymentMethod = paymentType === 'online' ? 'Online Payment' : `Cash on ${isGarage ? 'Service' : (deliveryType === 'delivery' ? 'Delivery' : 'Pickup')}`;
            const timeEstimate = isGarage ? '30-45 minutes' : (deliveryType === 'delivery' ? '25-30 minutes' : '15-20 minutes');
            const serviceType = isGarage ? 'Service Booking' : (deliveryType === 'delivery' ? 'Home Delivery' : 'Takeaway');
            alert(`🎉 ${isGarage ? 'Service booked' : 'Order placed'} successfully!\n\nService: ${serviceType}\nPayment: ${paymentMethod}\nTotal: ₹${total}\n\nYour ${isGarage ? 'service will be completed' : 'order will be ready'} in ${timeEstimate}.\nThank you for choosing ${vendorName}!`);
        }

        document.addEventListener('click', (e) => {
            if (e.target.id === 'cart-modal') closeCart();
            if (e.target.id === 'map-modal') closeMapModal();
        });

        // Load vendor reviews and display them
        function loadVendorReviews() {
            fetch(`/api/vendor/${vendorId}/reviews`)
                .then(res => res.json())
                .then(data => {
                    const avgRating = data.avg_rating || 0;
                    const reviewCount = data.review_count || 0;
                    
                    // Format rating with golden star in superscript
                    const ratingDisplay = avgRating > 0 ? 
                        `${avgRating}<span style="color:#ffd700;font-size:0.8em;vertical-align:super;">★</span>` : 
                        'New';
                    
                    document.getElementById('vendor-rating').innerHTML = ratingDisplay;
                    document.getElementById('reviews-avg-rating').innerHTML = avgRating > 0 ? avgRating : 'New';
                    document.getElementById('reviews-total-count').textContent = reviewCount;
                })
                .catch(() => {
                    document.getElementById('vendor-rating').innerHTML = 'New';
                    document.getElementById('reviews-avg-rating').innerHTML = 'New';
                    document.getElementById('reviews-total-count').textContent = '0';
                });
            
            // Load actual reviews
            fetch(`/api/vendor/${vendorId}/all-reviews`)
                .then(res => res.json())
                .then(reviews => {
                    const reviewsList = document.getElementById('reviews-list');
                    if (reviews.length === 0) {
                        reviewsList.innerHTML = `
                            <div style="text-align:center;padding:40px;color:#7f8c8d;">
                                <i class="fa-solid fa-star" style="font-size:48px;margin-bottom:15px;color:#ddd;"></i>
                                <h3>No Reviews Yet</h3>
                                <p>Be the first to review this vendor!</p>
                            </div>
                        `;
                    } else {
                        reviewsList.innerHTML = reviews.map(review => `
                            <div class="review-card">
                                <div style="display:flex;justify-content:space-between;margin-bottom:10px;">
                                    <strong>${review.customer_name}</strong>
                                    <span style="color:#f1c40f;">${'★'.repeat(review.rating)}${'☆'.repeat(5-review.rating)}</span>
                                </div>
                                <p style="color:#7f8c8d;margin:0;">${review.comment}</p>
                                <small style="color:#bdc3c7;margin-top:8px;display:block;">${new Date(review.date).toLocaleDateString()}</small>
                                ${review.vendor_response ? `
                                    <div style="background:#e3f2fd;padding:12px;border-radius:8px;margin-top:12px;border-left:3px solid #2196f3;">
                                        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:8px;">
                                            <strong style="color:#1976d2;font-size:14px;">Shop Response</strong>
                                            <small style="color:#7f8c8d;">${new Date(review.response_date).toLocaleDateString()}</small>
                                        </div>
                                        <p style="margin:0;color:#2c3e50;font-size:14px;">${review.vendor_response}</p>
                                        ${review.helpful_count > 0 ? `<small style="color:#27ae60;margin-top:8px;display:block;">👍 ${review.vendor_name} found this helpful</small>` : ''}
                                    </div>
                                ` : ''}
                            </div>
                        `).join('');
                    }
                })
                .catch(() => {
                    document.getElementById('reviews-list').innerHTML = `
                        <div style="text-align:center;padding:40px;color:#7f8c8d;">
                            <p>Unable to load reviews at this time.</p>
                        </div>
                    `;
                });
        }
        loadVendorReviews();
    </script>
</body>

</html>