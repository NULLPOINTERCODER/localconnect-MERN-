<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice #<%= order.id  %> - LocalConnect</title>
    <link rel="stylesheet" href="/static/css/customer/invoice.css">
    <link href="https://fonts.googleapis.com/css2?family=Segoe+UI:wght@400;600;700&display=swap" rel="stylesheet">
</head>

<body>
    <div class="invoice-container">
        <!-- Header Section -->
        <div class="invoice-header">
            <div class="company-info">
                <h1>LocalConnect</h1>
                <p class="tagline">Connecting You Locally</p>
            </div>
            <div class="invoice-info">
                <h2>INVOICE</h2>
                <p class="invoice-number">#<%= order.id  %></p>
                <p class="invoice-date"><%= order.created_at.strftime('%B %d, %Y')  %></p>
            </div>
        </div>

        <!-- Order Summary Cards -->
        <div class="summary-cards">
            <div class="summary-card">
                <div class="card-icon">📅</div>
                <div class="card-content">
                    <p class="card-label">Order Date</p>
                    <p class="card-value"><%= order.created_at.strftime('%d %b %Y, %I:%M %p')  %></p>
                </div>
            </div>
            <div class="summary-card">
                <div class="card-icon">✅</div>
                <div class="card-content">
                    <p class="card-label">Status</p>
                    <p class="card-value">Delivered</p>
                </div>
            </div>
            <div class="summary-card">
                <div class="card-icon">💳</div>
                <div class="card-content">
                    <p class="card-label">Payment Method</p>
                    <p class="card-value"><%= 'Online Payment' if order.payment_type == 'online' else 'Cash on ' +
                        ('Delivery' if order.delivery_type == 'delivery' else 'Pickup')  %></p>
                </div>
            </div>
            <div class="summary-card">
                <div class="card-icon">🛍️</div>
                <div class="card-content">
                    <p class="card-label">Total Items</p>
                    <p class="card-value"><%= item_count  %></p>
                </div>
            </div>
        </div>

        <!-- Party Details -->
        <div class="party-details">
            <div class="party-card vendor-card">
                <h3>From (Vendor)</h3>
                <div class="party-info">
                    <p class="party-name"><%= vendor.business_name  %></p>
                    <p><%= vendor.business_address  %></p>
                    <p><strong>Phone:</strong> <%= vendor.phone  %></p>
                    <p><strong>Email:</strong> <%= vendor.email  %></p>
                </div>
            </div>
            <div class="party-card customer-card">
                <h3>To (Customer)</h3>
                <div class="party-info">
                    <p class="party-name"><%= customer.full_name  %></p>
                    <% if (customer.address) { %>
                    <p><%= customer.address  %><% if (customer.city) { %>, <%= customer.city  %><% } %></p>
                    <% if (customer.state or customer.pincode) { %>
                    <p><% if (customer.state) { %><%= customer.state  %><% } %><% if (customer.pincode) { %> - <%= customer.pincode  %><% } %></p>
                    <% } %>
                    <% } else { %>
                    <p>Address not provided</p>
                    <% } %>
                    <p><strong>Phone:</strong> <%= customer.phone  %></p>
                    <p><strong>Email:</strong> <%= customer.email  %></p>
                </div>
            </div>
        </div>

        <!-- Items Table -->
        <div class="items-section">
            <h3>Order Items</h3>
            <table class="items-table">
                <thead>
                    <tr>
                        <th>Item Name</th>
                        <th>Quantity</th>
                        <th>Unit Price</th>
                        <th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (items && items.length) { items.forEach(function(item) { %>
                    <tr>
                        <td>
                            <span class="item-name"><%= item.name  %></span>
                            <% if (item.sub_name) { %>
                            <span class="item-subname"><%= item.sub_name  %></span>
                            <% } %>
                        </td>
                        <td><%= item.qty  %></td>
                        <td>₹<%= item.price  %></td>
                        <td>₹<%= item.price * item.qty  %></td>
                    </tr>
                    <% }); } %>
                    <% if (order.discount_amount and order.discount_amount > 0) { %>
                    <tr class="discount-row">
                        <td colspan="3">
                            <span class="discount-label">Discount Applied (<%= order.offer_title or 'Special Offer'  %>)</span>
                        </td>
                        <td class="discount-amount">-₹<%= order.discount_amount  %></td>
                    </tr>
                    <% } %>
                    <% if (order.delivery_type == 'delivery') { %>
                    <tr class="delivery-row">
                        <td colspan="3">
                            <span class="delivery-label">Home Delivery Charge</span>
                        </td>
                        <td class="delivery-amount">₹30.00</td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
        </div>

        <!-- Total Section -->
        <div class="total-section">
            <div class="total-card">
                <div class="total-row grand-total">
                    <span class="total-label">Grand Total</span>
                    <span class="total-amount">₹<%= order.total  %></span>
                </div>
                <div class="amount-words">
                    <p>Amount in words:</p>
                    <p class="words-text"><%= amount_words  %></p>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div class="invoice-footer">
            <div class="footer-note">
                <p><strong>Note:</strong> This is a computer-generated invoice and does not require a signature.</p>
                <p>Thank you for choosing LocalConnect!</p>
            </div>
        </div>

        <!-- Print Button (hidden in PDF) -->
        <div class="print-button-container no-print">
            <button onclick="window.print()" class="print-btn">
                <span>🖨️</span> Download Invoice (PDF)
            </button>
        </div>
    </div>

    <script>
        // Auto-focus for better UX
        window.onload = function () {
            document.body.classList.add('loaded');
        };
    </script>
</body>

</html>