<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile - LocalConnect</title>
    <link rel="stylesheet" href="/static/css/customer/base.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        .profile-container {
            max-width: 800px;
            margin: 40px auto;
            padding: 20px;
        }

        .profile-card {
            background: white;
            border-radius: 15px;
            padding: 40px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        }

        .profile-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .profile-avatar {
            width: 100px;
            height: 100px;
            background: #27ae60;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            color: white;
            font-size: 40px;
            font-weight: 700;
        }

        .profile-info {
            margin: 30px 0;
        }

        .info-row {
            display: flex;
            padding: 15px 0;
            border-bottom: 1px solid #f0f0f0;
        }

        .info-row:last-child {
            border-bottom: none;
        }

        .info-label {
            font-weight: 600;
            color: #7f8c8d;
            width: 150px;
        }

        .info-value {
            color: #2c3e50;
            flex: 1;
        }

        .profile-actions {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }

        .btn-action {
            flex: 1;
            padding: 15px;
            border: none;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn-edit {
            background: #27ae60;
            color: white;
        }

        .btn-edit:hover {
            background: #219150;
            transform: translateY(-2px);
        }

        .btn-complete {
            background: #f0f0f0;
            color: #2c3e50;
        }

        .btn-complete:hover {
            background: #e0e0e0;
        }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1000;
        }

        .modal.active {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            background: white;
            border-radius: 15px;
            padding: 40px;
            max-width: 600px;
            width: 90%;
            max-height: 90vh;
            overflow-y: auto;
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .close-modal {
            background: none;
            border: none;
            font-size: 24px;
            cursor: pointer;
            color: #7f8c8d;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 8px;
        }

        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
        }

        .form-group textarea {
            min-height: 80px;
            resize: vertical;
        }

        .location-btn {
            margin-top: 8px;
            padding: 10px 15px;
            background: #27ae60;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
        }

        .location-btn:hover {
            background: #219150;
        }

        .submit-btn {
            width: 100%;
            padding: 15px;
            background: #27ae60;
            color: white;
            border: none;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 20px;
        }

        .submit-btn:hover {
            background: #219150;
        }

        .camera-btn {
            margin-top: 8px;
            padding: 10px 15px;
            background: #3498db;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            margin-left: 10px;
        }

        .camera-btn:hover {
            background: #2980b9;
        }

        #cameraPreview {
            display: none;
            margin-top: 10px;
        }

        #cameraPreview video {
            width: 100%;
            max-width: 300px;
            border-radius: 10px;
        }

        .camera-controls {
            margin-top: 10px;
            display: flex;
            gap: 10px;
        }
    </style>
</head>

<body data-user-id="<%= user_id  %>">
    <header class="navbar">
        <div class="nav-container">
            <div class="logo" onclick="window.location.href='/customer/dashboard'">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"
                    stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"></path>
                    <circle cx="12" cy="10" r="3"></circle>
                </svg>
                <span>LocalConnect</span>
            </div>

            <div class="search-wrapper" id="searchWrapper">
                <input type="text" id="searchInput" placeholder="Search...">
                <svg class="search-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                    stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <circle cx="11" cy="11" r="8"></circle>
                    <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                </svg>
            </div>

            <div class="user-profile">
                <span class="welcome-text">Welcome, <%= (typeof user_name !== "undefined" && user_name) ? user_name : 'Guest' %></span>
                <div class="avatar" id="profileIcon">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"></path>
                        <circle cx="12" cy="7" r="4"></circle>
                    </svg>
                </div>
                <div class="profile-dropdown" id="profileDropdown">
                    <div class="dropdown-item" onclick="viewProfile()">
                        <i class="fa-solid fa-user"></i>
                        <span>View Profile</span>
                    </div>
                    <div class="dropdown-item" onclick="changePassword()">
                        <i class="fa-solid fa-key"></i>
                        <span>Change Password</span>
                    </div>
                    <div class="dropdown-item" onclick="myOrders()">
                        <i class="fa-solid fa-shopping-bag"></i>
                        <span>My Orders</span>
                    </div>
                    <div class="dropdown-item" onclick="logout()">
                        <i class="fa-solid fa-sign-out-alt"></i>
                        <span>Logout</span>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Complete Profile Modal -->
    <div class="modal" id="completeProfileModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2>Complete Your Profile</h2>
                <button class="close-modal" onclick="closeCompleteModal()">&times;</button>
            </div>
            <form id="completeProfileForm" onsubmit="submitCompleteProfile(event)">
                <div class="form-group">
                    <label>Delivery Address *</label>
                    <textarea id="deliveryAddress" placeholder="House/Flat No, Street, Area" required></textarea>
                    <button type="button" class="location-btn" onclick="getCurrentLocation()">
                        <i class="fa-solid fa-location-crosshairs"></i> Use Current Location
                    </button>
                </div>
                <div class="form-group">
                    <label>City *</label>
                    <input type="text" id="city" placeholder="Enter your city" required>
                </div>
                <div class="form-group">
                    <label>State *</label>
                    <input type="text" id="state" placeholder="Enter your state" required>
                </div>
                <div class="form-group">
                    <label>PIN Code *</label>
                    <input type="text" id="pincode" placeholder="Enter PIN code" pattern="[0-9]{6}" required>
                </div>
                <div class="form-group">
                    <label>Alternate Phone (Optional)</label>
                    <input type="tel" id="altPhone" placeholder="Alternate contact number">
                </div>
                <div class="form-group">
                    <label>Preferred Delivery Time</label>
                    <select id="deliveryTime">
                        <option value="">Select preferred time</option>
                        <option value="morning">Morning (9 AM - 12 PM)</option>
                        <option value="afternoon">Afternoon (12 PM - 4 PM)</option>
                        <option value="evening">Evening (4 PM - 8 PM)</option>
                        <option value="night">Night (8 PM - 11 PM)</option>
                    </select>
                </div>
                <button type="submit" class="submit-btn">
                    <i class="fa-solid fa-check"></i> Save & Complete Profile
                </button>
            </form>
        </div>
    </div>

    <main class="profile-container">
        <div class="profile-card">
            <div class="profile-header">
                <div class="profile-avatar" id="profileAvatar"></div>
                <h2><%= customer.full_name if customer else 'User'  %></h2>
                <p style="color: #7f8c8d;">Customer Profile</p>
            </div>

            <div class="profile-info">
                <div class="info-row">
                    <div class="info-label">Full Name</div>
                    <div class="info-value"><%= customer.full_name if customer else 'Not provided'  %></div>
                </div>
                <div class="info-row">
                    <div class="info-label">Email</div>
                    <div class="info-value"><%= customer.email if customer else 'Not provided'  %></div>
                </div>
                <div class="info-row">
                    <div class="info-label">Phone</div>
                    <div class="info-value"><%= customer.phone if customer else 'Not provided'  %></div>
                </div>
                <div class="info-row">
                    <div class="info-label">Address</div>
                    <div class="info-value"><%= customer.address if customer and customer.address else 'Not provided'  %>
                    </div>
                </div>
                <div class="info-row">
                    <div class="info-label">City</div>
                    <div class="info-value"><%= customer.city if customer and customer.city else 'Not provided'  %></div>
                </div>
                <div class="info-row">
                    <div class="info-label">State</div>
                    <div class="info-value"><%= customer.state if customer and customer.state else 'Not provided'  %>
                    </div>
                </div>
                <div class="info-row">
                    <div class="info-label">PIN Code</div>
                    <div class="info-value"><%= customer.pincode if customer and customer.pincode else 'Not provided'  %>
                    </div>
                </div>
                <div class="info-row">
                    <div class="info-label">Home Location</div>
                    <div class="info-value">
                        <% if (customer and customer.home_latitude and customer.home_longitude) { %>
                        <span>📍 <%= customer.home_latitude  %>, <%= customer.home_longitude  %></span>
                        <% } else { %>
                        <span>Not set</span>
                        <% } %>
                        <button onclick="setHomeLocation()"
                            style="margin-left:10px;padding:5px 10px;background:#27ae60;color:white;border:none;border-radius:5px;cursor:pointer;"><%= 'Update' if customer and customer.home_latitude else 'Set'  %> Home Location</button>
                    </div>
                </div>
                <div class="info-row">
                    <div class="info-label">Current Location</div>
                    <div class="info-value">
                        <% if (customer and customer.current_latitude and customer.current_longitude) { %>
                        <span>📍 <%= customer.current_latitude  %>, <%= customer.current_longitude  %></span>
                        <% } else { %>
                        <span>Not set</span>
                        <% } %>
                        <button onclick="updateCurrentLocation()"
                            style="margin-left:10px;padding:5px 10px;background:#e67e22;color:white;border:none;border-radius:5px;cursor:pointer;">Update
                            Current</button>
                    </div>
                </div>
                <div class="info-row">
                    <div class="info-label">Member Since</div>
                    <div class="info-value"><%= customer.id if customer else 'N/A'  %></div>
                </div>
            </div>

            <div class="profile-actions">
                <button class="btn-action btn-edit" onclick="toggleEditMode()">
                    <i class="fa-solid fa-edit"></i> Edit Profile
                </button>
                <button class="btn-action btn-complete" onclick="completeProfile()">
                    <i class="fa-solid fa-check-circle"></i> Complete Profile
                </button>
                <button class="btn-action" style="background: #e74c3c; color: white;" onclick="deleteAccount()">
                    <i class="fa-solid fa-trash"></i> Delete Account
                </button>
            </div>
        </div>
    </main>

    <script src="/static/js/customer/base.js"></script>
    <script>
        // Global functions for profile dropdown
        function viewProfile() {
            window.location.href = '/customer/profile';
        }

        function myOrders() {
            window.location.href = '/customer/orders';
        }

        function changePassword() {
            window.location.href = '/customer/change-password';
        }

        function logout() {
            // Clear any local storage or session storage
            if (typeof (Storage) !== "undefined") {
                localStorage.clear();
                sessionStorage.clear();
            }

            // Redirect to logout endpoint
            window.location.href = '/logout';
        }

        const userId = '<%= user_id  %>';

        // Clear ALL localStorage if different user logs in
        const lastUserId = localStorage.getItem('lastUserId');
        if (lastUserId && lastUserId !== userId) {
            localStorage.clear();
        }
        localStorage.setItem('lastUserId', userId);

        const profileImageKey = `user_${userId}_profileImage`;
        const userAddressKey = `user_${userId}_userAddress`;
        const userLocationKey = `user_${userId}_userLocation`;
        const completeProfileKey = `user_${userId}_completeProfile`;
        let editMode = false;

        // Load and display saved profile image on page load
        window.addEventListener('DOMContentLoaded', () => {
            const savedImage = localStorage.getItem(profileImageKey);
            const avatar = document.getElementById('profileAvatar');
            if (savedImage) {
                avatar.style.backgroundImage = `url(${savedImage})`;
                avatar.style.backgroundSize = 'cover';
                avatar.style.backgroundPosition = 'center';
                avatar.textContent = '';
            } else {
                avatar.textContent = '<%= customer.full_name|first|upper if customer else "U"  %>';
            }
        });

        function toggleEditMode() {
            editMode = !editMode;
            const profileInfo = document.querySelector('.profile-info');
            const btn = document.querySelector('.btn-edit');

            if (editMode) {
                btn.innerHTML = '<i class="fa-solid fa-save"></i> Save Changes';
                btn.onclick = saveProfile;
                profileInfo.innerHTML = `
                    <div class="info-row">
                        <div class="info-label">Profile Image</div>
                        <div class="info-value">
                            <input type="file" id="profileImage" accept="image/*" onchange="previewImage(event)" style="margin-bottom:10px;">
                            <button type="button" class="camera-btn" onclick="openCamera()">
                                <i class="fa-solid fa-camera"></i> Take Photo
                            </button>
                            <div id="cameraPreview">
                                <video id="cameraVideo" autoplay></video>
                                <div class="camera-controls">
                                    <button type="button" class="location-btn" onclick="capturePhoto()">
                                        <i class="fa-solid fa-camera"></i> Capture
                                    </button>
                                    <button type="button" class="camera-btn" onclick="closeCamera()">
                                        <i class="fa-solid fa-times"></i> Cancel
                                    </button>
                                </div>
                            </div>
                            <div id="imagePreview" style="display:none;margin-top:10px;">
                                <img id="preview" style="width:100px;height:100px;border-radius:50%;object-fit:cover;">
                            </div>
                        </div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">Full Name</div>
                        <div class="info-value"><input type="text" id="fullName" value="<%= customer.full_name if customer else ''  %>" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:5px;"></div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">Email</div>
                        <div class="info-value"><input type="email" id="email" value="<%= customer.email if customer else ''  %>" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:5px;"></div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">Phone</div>
                        <div class="info-value"><input type="tel" id="phone" value="<%= customer.phone if customer else ''  %>" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:5px;"></div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">Address</div>
                        <div class="info-value">
                            <textarea id="address" placeholder="Enter your delivery address" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:5px;min-height:60px;resize:vertical;"><%= customer.address if customer else ''  %></textarea>
                            <button onclick="getCurrentLocation()" style="margin-top:8px;padding:8px 12px;background:#27ae60;color:white;border:none;border-radius:5px;cursor:pointer;">
                                <i class="fa-solid fa-location-crosshairs"></i> Use Current Location
                            </button>
                        </div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">City</div>
                        <div class="info-value"><input type="text" id="city" value="<%= customer.city if customer else ''  %>" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:5px;"></div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">State</div>
                        <div class="info-value"><input type="text" id="state" value="<%= customer.state if customer else ''  %>" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:5px;"></div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">PIN Code</div>
                        <div class="info-value"><input type="text" id="pincode" value="<%= customer.pincode if customer else ''  %>" pattern="[0-9]{6}" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:5px;"></div>
                    </div>
                `;
            } else {
                location.reload();
            }
        }

        function previewImage(event) {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    document.getElementById('imagePreview').style.display = 'block';
                    document.getElementById('preview').src = e.target.result;
                };
                reader.readAsDataURL(file);
            }
        }

        function saveProfile() {
            const fullName = document.getElementById('fullName').value;
            const email = document.getElementById('email').value;
            const phone = document.getElementById('phone').value;
            const address = document.getElementById('address').value;
            const city = document.getElementById('city').value;
            const state = document.getElementById('state').value;
            const pincode = document.getElementById('pincode').value;
            const imageFile = document.getElementById('profileImage').files[0];

            // Prepare data for backend
            const profileData = {
                full_name: fullName,
                email: email,
                phone: phone,
                address: address,
                city: city,
                state: state,
                pincode: pincode
            };

            // Send to backend
            fetch('/api/customer/profile', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(profileData)
            })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        // Determine final image data
                        let finalImageData = capturedImageData || null;

                        const saveImageAndUpdate = (imgData) => {
                            if (imgData) {
                                localStorage.setItem(profileImageKey, imgData);
                                // Update navbar avatar instantly
                                const navAvatar = document.getElementById('profileIcon');
                                if (navAvatar) {
                                    navAvatar.innerHTML = `<img src="${imgData}" alt="Profile"
                                        style="width:100%;height:100%;border-radius:50%;object-fit:cover;display:block;">`;
                                }
                            }
                            if (address) localStorage.setItem(userAddressKey, address);
                            alert('Profile updated successfully!');
                            location.reload();
                        };

                        if (finalImageData) {
                            saveImageAndUpdate(finalImageData);
                        } else if (imageFile) {
                            const reader = new FileReader();
                            reader.onload = (e) => saveImageAndUpdate(e.target.result);
                            reader.readAsDataURL(imageFile);
                        } else {
                            saveImageAndUpdate(null);
                        }
                    } else {
                        alert('Error updating profile: ' + (data.error || 'Unknown error'));
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('Network error. Please try again.');
                });
        }

        function getCurrentLocation() {
            const addressField = document.getElementById('deliveryAddress') || document.getElementById('address');
            if (!addressField) {
                alert('Address field not found');
                return;
            }

            if (!navigator.geolocation) {
                alert('Geolocation is not supported by your browser.');
                return;
            }

            addressField.value = 'Getting location...';
            navigator.geolocation.getCurrentPosition(
                (position) => {
                    const lat = position.coords.latitude;
                    const lng = position.coords.longitude;

                    fetch(`https://nominatim.openstreetmap.org/reverse?format=json&lat=${lat}&lon=${lng}`)
                        .then(res => res.json())
                        .then(data => {
                            if (data.address) {
                                const addr = data.address;
                                const street = addr.road || addr.suburb || addr.neighbourhood || '';
                                const area = addr.city || addr.town || addr.village || '';
                                addressField.value = street ? `${street}, ${area}` : data.display_name;

                                const cityField = document.getElementById('city');
                                const stateField = document.getElementById('state');
                                const pincodeField = document.getElementById('pincode');

                                if (cityField) cityField.value = addr.city || addr.town || addr.village || '';
                                if (stateField) stateField.value = addr.state || '';
                                if (pincodeField) pincodeField.value = addr.postcode || '';
                            } else {
                                addressField.value = data.display_name || `${lat.toFixed(6)}, ${lng.toFixed(6)}`;
                            }
                            localStorage.setItem(userLocationKey, JSON.stringify({ lat, lng }));
                        })
                        .catch(() => {
                            addressField.value = `${lat.toFixed(6)}, ${lng.toFixed(6)}`;
                            alert('Could not fetch address. Please enter manually.');
                        });
                },
                (error) => {
                    addressField.value = '';
                    alert(error.code === 1 ? 'Location permission denied' : 'Unable to get location');
                }
            );
        }

        function completeProfile() {
            document.getElementById('completeProfileModal').classList.add('active');
            const savedData = JSON.parse(localStorage.getItem(completeProfileKey) || '{}');
            if (savedData.address) document.getElementById('deliveryAddress').value = savedData.address;
            if (savedData.city) document.getElementById('city').value = savedData.city;
            if (savedData.state) document.getElementById('state').value = savedData.state;
            if (savedData.pincode) document.getElementById('pincode').value = savedData.pincode;
            if (savedData.altPhone) document.getElementById('altPhone').value = savedData.altPhone;
            if (savedData.deliveryTime) document.getElementById('deliveryTime').value = savedData.deliveryTime;
        }

        function closeCompleteModal() {
            document.getElementById('completeProfileModal').classList.remove('active');
        }

        function submitCompleteProfile(e) {
            e.preventDefault();
            const profileData = {
                address: document.getElementById('deliveryAddress').value,
                city: document.getElementById('city').value,
                state: document.getElementById('state').value,
                pincode: document.getElementById('pincode').value
            };

            // Send to backend
            fetch('/api/customer/profile', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(profileData)
            })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        // Also save to localStorage for immediate use
                        const completeData = {
                            ...profileData,
                            altPhone: document.getElementById('altPhone').value,
                            deliveryTime: document.getElementById('deliveryTime').value,
                            completedAt: new Date().toISOString()
                        };
                        localStorage.setItem(completeProfileKey, JSON.stringify(completeData));

                        alert('Profile completed successfully! You can now enjoy full delivery services.');
                        closeCompleteModal();
                        location.reload();
                    } else {
                        alert('Error updating profile: ' + (data.error || 'Unknown error'));
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('Network error. Please try again.');
                });
        }

        let cameraStream = null;
        let capturedImageData = null;

        function openCamera() {
            const preview = document.getElementById('cameraPreview');
            const video = document.getElementById('cameraVideo');

            navigator.mediaDevices.getUserMedia({ video: true })
                .then(stream => {
                    cameraStream = stream;
                    video.srcObject = stream;
                    preview.style.display = 'block';
                })
                .catch(() => alert('Unable to access camera. Please check permissions.'));
        }

        function closeCamera() {
            if (cameraStream) {
                cameraStream.getTracks().forEach(track => track.stop());
                cameraStream = null;
            }
            document.getElementById('cameraPreview').style.display = 'none';
        }

        function capturePhoto() {
            const video = document.getElementById('cameraVideo');
            const canvas = document.createElement('canvas');
            canvas.width = video.videoWidth;
            canvas.height = video.videoHeight;
            canvas.getContext('2d').drawImage(video, 0, 0);

            capturedImageData = canvas.toDataURL('image/jpeg');
            document.getElementById('preview').src = capturedImageData;
            document.getElementById('imagePreview').style.display = 'block';
            closeCamera();
        }

        function deleteAccount() {
            if (!confirm('Are you sure you want to permanently delete your account? This action cannot be undone!')) {
                return;
            }

            if (!confirm('This will delete all your orders, reviews, and personal data. Type "DELETE" to confirm:')) {
                return;
            }

            const userInput = prompt('Type "DELETE" to confirm account deletion:');
            if (userInput !== 'DELETE') {
                alert('Confirmation failed. Account deletion cancelled.');
                return;
            }

            fetch('/customer/delete-account', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                }
            })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        alert('Account deleted successfully. You will be redirected to the home page.');
                        localStorage.clear();
                        sessionStorage.clear();
                        window.location.href = data.redirect;
                    } else {
                        alert('Error deleting account: ' + (data.error || 'Unknown error'));
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('Network error. Please try again.');
                });
        }

        function setHomeLocation() {
            if (!navigator.geolocation) {
                alert('Geolocation is not supported by your browser.');
                return;
            }

            navigator.geolocation.getCurrentPosition(
                (position) => {
                    const lat = position.coords.latitude;
                    const lng = position.coords.longitude;

                    fetch('/api/customer/set-home-location', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({
                            home_latitude: lat,
                            home_longitude: lng
                        })
                    })
                        .then(response => response.json())
                        .then(data => {
                            if (data.success) {
                                alert('Home location set successfully!');
                                location.reload();
                            } else {
                                alert('Error setting home location: ' + (data.error || 'Unknown error'));
                            }
                        })
                        .catch(error => {
                            console.error('Error:', error);
                            alert('Network error. Please try again.');
                        });
                },
                (error) => {
                    alert(error.code === 1 ? 'Location permission denied' : 'Unable to get location');
                }
            );
        }

        function updateCurrentLocation() {
            if (!navigator.geolocation) {
                alert('Geolocation is not supported by your browser.');
                return;
            }

            navigator.geolocation.getCurrentPosition(
                (position) => {
                    const lat = position.coords.latitude;
                    const lng = position.coords.longitude;

                    fetch('/api/customer/update-current-location', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({
                            current_latitude: lat,
                            current_longitude: lng
                        })
                    })
                        .then(response => response.json())
                        .then(data => {
                            if (data.success) {
                                alert('Current location updated successfully!');
                                location.reload();
                            } else {
                                alert('Error updating current location: ' + (data.error || 'Unknown error'));
                            }
                        })
                        .catch(error => {
                            console.error('Error:', error);
                            alert('Network error. Please try again.');
                        });
                },
                (error) => {
                    alert(error.code === 1 ? 'Location permission denied' : 'Unable to get location');
                }
            );
        }


    </script>
</body>

</html>