<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password | LocalConnect</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/static/css/sign_in.css">
</head>
<body>
    <div class="bg-grid"></div>
    
    <div class="signin-container">
        <div class="signin-card">
            <header class="card-header">
                <h1>Change <span class="brand-gradient">Password</span></h1>
                <p class="subtitle">Enter your current and new password</p>
            </header>

            {# with messages = get_flashed_messages(with_categories=true) #}
                <% if (messages && messages.length) { %>
                    <% messages.forEach(([category, message]) => { %>
                        <div class="alert alert-<%= category === "success" ? "success" : "error" %>">
                            <%= message %>
                        </div>
                    <% })); } %>
                <% } %>
            

            <form class="signin-form" method="POST" action="<%= "/change_password"  %>">
                <div class="input-group">
                    <label class="input-label">* Current Password</label>
                    <input type="password" name="current_password" placeholder="Enter current password" required>
                </div>

                <div class="input-group">
                    <label class="input-label">* New Password</label>
                    <input type="password" name="new_password" placeholder="Enter new password" required>
                </div>

                <div class="input-group">
                    <label class="input-label">* Confirm New Password</label>
                    <input type="password" name="confirm_password" placeholder="Confirm new password" required>
                </div>

                <div class="button-row">
                    <button type="submit" class="btn-login">Update Password</button>
                </div>
            </form>

            <footer class="card-footer">
                <p>Go back to <a href="/customer/profile">Profile</a></p>
            </footer>
        </div>
    </div>
</body>
</html>